# NeuroStation 项目验收报告

验收日期：2026-09-11（北京时间）
验收环境：Windows 11 x64，Python 3.14.5，PySide6 6.11.2，BrainFlow 5.22.2
交付定位：科研/教学技术验证工具，不作为医疗诊断设备

## 结论

当前版本可标记为：**Windows 无硬件功能 MVP / Windows standalone 验收通过**。

工作站已经形成完整的本地采集链路：中文优先 PySide6 工作站、SSVEP 采集应用入口、参数计算、5 秒倒计时、全屏刺激 worker、BrainFlow Synthetic/Cyton 模式、会话文件、事件、帧时序和质量日志、数据集导航，以及 OpenBCI GUI 锁定源码和中文 overlay 的受管启动。

当前版本**不能**标记为“Cyton SSVEP 正式候选版”或“三平台 beta 发布版”。原因是实际 Cyton 硬件、photodiode/TTL 光学时序、Linux/macOS 原生构建和安装器人工验收尚未有本机证据。

## 已通过证据

| 类别 | 证据 | 结果 |
| --- | --- | --- |
| 核心/集成测试 | `python -m unittest discover -s tests -v` | 20/20 通过 |
| 独立 UI 测试 | `python -m unittest discover -s apps/workstation_ui/tests -v` | 21/21 通过 |
| 语法与格式 | `compileall`、`git diff --check` | 通过 |
| BrainFlow synthetic | 中文路径、原始 TSV、事件、session、quality、manifest | 通过 |
| 取消与恢复 | countdown/run 中止后保存 aborted 会话 | 通过 |
| OpenBCI GUI | 锁定 revision `e23869e7b5cc621e733d8fa0d81f05d477264306`、中文 overlay、Windows Processing 导出 | 通过 |
| Windows standalone | `dist/NeuroStation.dist/workstation.exe` 启动、诊断、worker smoke（含 quality/manifest） | 通过 |
| 启动预检 | `workstation.exe --diagnostics` 输出平台、依赖、资源、配置、OpenBCI 状态 | 通过 |
| 窄布局 | 320×560 与 1024×640 Qt offscreen 窗口测试 | 通过；高 DPI 实机待验收 |
| 硬件失败提示 | 无效 Cyton 端口返回结构化 JSON、退出码 3 和可操作提示 | 通过 |

2026-09-11 最终打包复核：`scripts/smoke_packaged.py` 使用
`dist/NeuroStation.dist/workstation.exe` 完成离屏启动、打包资源诊断和
BrainFlow Synthetic worker 端到端采集；校验了中文数据集路径、非空
`raw_brainflow.tsv`、`quality.json`、`session.json` 以及 `manifest.csv` 对质量和
配置副本的引用，退出码为 0。随后直接运行打包的
`workstation.exe --diagnostics`，并再次完成 `compileall` 与 `git diff --check`。

## 运行方式

启动工作站：

```powershell
.\.venv\Scripts\python.exe workstation.py
```

启动已打包 Windows 版本：

```powershell
.\dist\NeuroStation.dist\workstation.exe
```

运行不打开串口的预检：

```powershell
.\dist\NeuroStation.dist\workstation.exe --diagnostics
```

运行真实 Cyton 前，先创建并审核正式通道表，然后执行：

```powershell
Copy-Item configs\channel_config_v1_template.json configs\channel_config_v1.json
python run_ssvep_session.py --channel-config configs\channel_config_v1.json --validate-only
python check_cyton_live.py --port COM5 --seconds 15
```

## 未完成门槛

1. H-01～H-07：需要 OpenBCI Cyton、USB dongle、实际 8 通道接线和三平台测试机；
2. T-01～T-03：需要实际显示器刷新率报告和 photodiode/TTL 仪器，软件逐帧日志不能替代物理测量；
3. D-01～D-07 的 Linux/macOS 列：需要对应系统原生构建、安装/升级/卸载和人工布局验收；
4. OpenBCI GUI 完整中文化：当前为首批核心 overlay，仍需按 `integrations/openbci_gui/I18N_ZH_CN.md` 扩展到全部用户可见页面；
5. 正式发布：仍需签名、notarization、安装器、SBOM 和第三方许可证清单。

## 放行规则

- A 系列全部通过：可作为功能 MVP；
- 对应平台 D 系列全部通过：才可发布该平台 beta 安装包；
- H 与 T 系列全部通过：才可标记为 Cyton SSVEP 正式候选版；
- Synthetic、Demo 必须继续显著标记 `SIMULATED`，不得将模拟样本数解释为真实 EEG。
